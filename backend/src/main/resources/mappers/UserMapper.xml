<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.suhyeon.intelli_folio.module.user.UserMapper">

    <select id="findByEmail" parameterType="string" resultType="com.suhyeon.intelli_folio.module.user.domain.User">
        SELECT
            id,
            email,
            password_hash AS passwordHash,
            display_name AS displayName,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM users
        WHERE email = #{email}
            LIMIT 1
    </select>

    <insert id="insert"
            parameterType="com.suhyeon.intelli_folio.module.user.dto.CreateUserCommand"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO users (email, password_hash, display_name)
        VALUES (#{email}, #{passwordHash}, #{displayName})
    </insert>


</mapper>