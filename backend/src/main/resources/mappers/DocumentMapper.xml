<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.suhyeon.intelli_folio.module.project.DocumentMapper">

    <insert id="insert"
            parameterType="com.suhyeon.intelli_folio.module.project.dto.CreateDocumentCommand"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO documents (project_id, title, original_filename, content_type, size_bytes)
        VALUES (#{projectId}, #{title}, #{originalFilename}, #{contentType}, #{sizeBytes})
    </insert>


    <select id="findByProjectIds"
            resultType="com.suhyeon.intelli_folio.module.project.domain.Document">
        SELECT
        id,
        project_id AS projectId,
        title,
        original_filename AS originalFilename,
        size_bytes AS sizeBytes,
        created_at AS createdAt,
        updated_at AS udpatedAt
        FROM documents
        WHERE project_id IN
        <foreach collection="projectIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
        ORDER BY project_id ASC, id ASC
    </select>

</mapper>